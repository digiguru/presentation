{
  "name": "digiguru-presentation-pure",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "lint": "eslint --config ../eslint.config.mjs ../scripts ../test/*.test.mjs src build test *.mjs",
    "test": "node --test test/*.test.mjs",
    "audit:legacy": "node build/audit-legacy-decks.mjs",
    "build": "vite build && node build/verify-build.mjs",
    "smoke": "node build/smoke.mjs",
    "check": "npm run test && npm run audit:legacy && npm run build"
  },
  "dependencies": {
    "reveal.js": "6.0.1"
  },
  "devDependencies": {
    "eslint": "10.8.1",
    "vite": "8.2.1"
  }
}
