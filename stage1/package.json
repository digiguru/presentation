{
  "name": "digiguru-presentation-stage1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "test": "node --test test/*.test.mjs",
    "build": "vite build && node build/verify-build.mjs",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "reveal.js": "6.0.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
